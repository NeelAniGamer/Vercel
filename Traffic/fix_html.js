const fs = require('fs'); const file = 'c:/Users/neelg/OneDrive/Desktop/Vercel/Traffic/TrafficDashboard.html'; let content = fs.readFileSync(file, 'utf8'); const rx = /\}ge_count\} badge\$\{[\s\S]*?\} catch \(e\) \{ const el = document.getElementById\('leaderboard-badges'\); if \(el\) el\.innerHTML = ERROR_MSG \}/; if (rx.test(content)) { content = content.replace(rx, '} catch (e) { const el = document.getElementById(\'leaderboard-badges\'); if (el) el.innerHTML = ERROR_MSG }'); fs.writeFileSync(file, content, 'utf8'); console.log('Fixed syntax error successfully.'); } else { console.log('Regex match failed'); }
