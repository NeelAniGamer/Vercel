const fs = require('fs'); const vm = require('vm'); const html = fs.readFileSync('Academy.html', 'utf8'); const regex = /<script\b[^>]*>([\s\S]*?)<\/script>/gi; let match; let i = 0; while ((match = regex.exec(html)) !== null) { try { new vm.Script(match[1]); console.log('Script ' + i + ' OK'); } catch(e) { console.log('Script ' + i + ' Error:', e.message); const lines = match[1].split('\n'); console.log('Line 1:', lines[0]); } i++; }
